
server.port=8085

# Disable Thymeleaf cache
spring.thymeleaf.cache=false

# Watch modifications on Thymeleaf templates and static content
#spring.devtools.restart.additional-paths=src/main/resources/static, src/main/resources/templates, src/main/resources/templates/fragments, src/main/resources/templates/pages
spring.devtools.restart.additional-paths=src/main/resources/templates, src/main/resources/static

# Logs
logging.level.root=INFO
server.tomcat.basedir=tomcat-logs
server.tomcat.accesslog.enabled=true
server.tomcat.accesslog.pattern=%t %a "%r" %s (%D ms)

# Database settings
spring.datasource.url= jdbc:mysql://localhost:3306/abcmapfr
spring.datasource.username=${ABCMAP_FR_DB_USERNAME}
spring.datasource.password=${ABCMAP_FR_DB_PASSWORD}

#spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.ddl-auto=create-drop

app.google-analytics-api-key = ${ABCMAP_FR_GANALYTICS_API_KEY}

app.mail-server = ${ABCMAP_FR_MAIL_SERVER}
app.mail-username = ${ABCMAP_FR_MAIL_USERNAME}
app.mail-password = ${ABCMAP_FR_MAIL_PASSWORD}

